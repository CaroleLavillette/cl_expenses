import React from "react";

export default class SubTitle extends React.Component {
    render() {
        return (
            <div className="subTitle">{this.props.subTitle}
            </div>
        )
    }
}